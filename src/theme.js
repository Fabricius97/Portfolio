// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ffffff", // Sätt primärfärgen till vitt
    },
  },
});

export default theme;
